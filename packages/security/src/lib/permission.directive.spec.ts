import { NdkPermissionDirective } from './permission.directive';

describe('NdkPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new NdkPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
